<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.ac.dongyang.n15_20202838">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- MainActivity는 LAUNCHER로 설정되어 있으므로 android:exported="true"로 설정합니다. -->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 다른 액티비티들도 android:exported 속성을 추가합니다. -->
        <activity android:name=".CoronaActivity"
            android:exported="false" />
        <activity android:name=".StateActivity"
            android:exported="false" />
        <activity android:name=".VideoActivity"
            android:exported="false" />
        <activity android:name=".DistancingActivity"
            android:exported="false" />

    </application>

</manifest>